``os.path.join``の仕様により、成分のいずれかが絶対パスの場合、それより前の引数はすべて破棄されてしまう。

そのため、以下のペイロードを入力する。

```
/flag.txt
```

こうすると、``os.path.join("/app", "/flag.txt")``が実行されるが、``/app``が破棄されるので、結局実行されるコマンドは``cat /flag.txt``となる。