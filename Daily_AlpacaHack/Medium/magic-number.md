``if``文を``True``側に持っていくとFLAGが得られそうなものだが、それは現実的ではない。インジェクションを考える。

``/*code*/``が``payload``に置換されるので、``payload``を以下のものとすると、``code``の1行目の段階でFLAGが``print``される。

```
print('/*flag*/')
```

その後``if``分は``else``側に進むが、あまり関係ない。