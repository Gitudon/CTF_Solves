`theta`として極端に大きな値を入力する。

`fsin`関数は、2^63以上の値を入力すると計算を放棄し、入力をそのまま返す。

これに対して`sin`関数は入力がどれほど大きくても適切に引数を削減し、正しい答えを返す。

なので、以下のような値を入力すると、両者の答えが極端に異なることとなり、フラグを得られる。

```
theta> 92200000000000000000
sine_1: -0.632166
sine_2: 9.22e+19
Unbelievable!
```